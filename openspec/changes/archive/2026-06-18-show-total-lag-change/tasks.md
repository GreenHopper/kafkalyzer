## 1. UI & Calculations

- [x] 1.1 Sum individual group lag deltas from `_groupDeltas` in `_buildKpiCards`
- [x] 1.2 Format the aggregate lag change subtitle with locale-specific separators and sign prefix
- [x] 1.3 Update the Total Lag `KpiCard` inside `_buildKpiCards` to display this subtitle

## 2. Verification & Testing

- [x] 2.1 Update `consumer_lag_view_test.dart` to verify that the aggregate lag change subtitle is rendered
- [x] 2.2 Verify that all project tests compile and pass successfully
